name: dsync services

on:
  push:
    branches:
      - master
      - 'fix/**'
  workflow_dispatch:
    inputs:
      commitMessage:
        description: commit
        required: true

jobs:
  PostgreSQL-Tests:
    if: " ${{ github.event.inputs.commitMessage }}!='[skip tests]' "
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: adopt
          java-version: 11
      - name: print the staement
        run: echo ${GITHUB_REF_NAME}

  Deploy:
    if: startsWith(github.ref_name, 'master')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: adopt
          java-version: 11

      - name: ${{ github.event.inputs.commitMessage }} Run script
        run: echo "hEllo world"